- name: Installing nginx package
  hosts: Node1
  become: yes
  tasks:
  - name: Install nginx 
    ansible.builtin.package:
      name: nginx
      state: Installed

  - name: Start nginx server
    ansible.buitlin.service:
      name: nginx
      state: started
      enable: yes

